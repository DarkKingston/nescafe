# Changelog

## 2.0.1
- Improvement css for better practices.
## 2.0.0
- Refactor image component.

## 1.4.3
- H's should be implemented inside the DIV instead of above.

## 1.4.2

- rel attribute added if target exist on anchor tag.
- Bug fix : Made full button clickable.

##Validation
- Add the component on a page and validate.

## 1.4.1

- Bug fix : CTA buttons adds default inline CSS when NONE selected for color.


##Validation
- Add the component on a page and validate.

## 1.3.4 for Drupal 9

- Enable the field_group module if not enabled.
- Go to '/admin/config/development/features/diff/dsu_c_image' and import the diff.
	- Update the widget of 'CTA Button URL' field from 'Link (with attributes)' to 'Link with target' on 'Manage form display'.
- Uninstall the link_target module.


##Validation
- Add the component on a page and validate.
